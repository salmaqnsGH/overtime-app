<% @posts.each do |post| %>
    <tr>
        <th scope="row"><%= post.id %></th>
        <td><%= post.date %></td>
        <td><%= truncate(post.rationale) %></td>
        <td>@<%= post.user_id %></td>
    </tr>
<% end %>